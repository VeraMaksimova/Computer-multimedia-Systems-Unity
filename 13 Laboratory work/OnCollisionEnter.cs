using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class OnCollisionEnter : MonoBehaviour
{

    /*5.	Добавить в программный код этого объекта добавить функцию OnCollisionEnter(Collision col), которая за-дает действия, происходящие при столкновении коллайдеров взаимодействующих объектов, а именно из-менение цвета объектов Cube1, Cube2 при столкновении с объектом Capsule, используя обращение к цвету объекта, с которым произошло столкновение в виде
  col.gameObject.GetComponent<Renderer>().material.color 
*/
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    
}
